# 暴力解
```java
class Solution{
  public int removeElement(int[] nums, int val){
    int n = nums.length;
    for(int i = 0; i < n;i++){
      if(nums[i] == val){
        for(int j = i + 1; j < n; J++){
          nums[j - 1] = nums[j];
        }
        i--;
        n--;
      }
    }
    return n ;
  }
)
```

# 快慢指针
```java
class Solution{
  public int removeElement(int[] nums, int val){
    int slowIndex = 0;
    for(int fastIndex = 0; fastIndex < nums.length; fastIndex++){
      if(nums[fastIndex]! = val){ //当找不到目标值时，慢指针才移动
        nums[slowIndex] = nums[fastIndex];
        slowIndex++;
      }
    }
    return slowIndex;
  }
}
```


#相向双指针法
```java
```
